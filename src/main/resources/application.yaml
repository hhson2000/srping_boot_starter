
spring:
  datasource:
    username: root
    password: root
    url: "jdbc:mysql://localhost:3306/identity"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  servlet:
    context-path: /identity
  port: 8080